services:
  unlimited-1:
    build:
      context: ..
      dockerfile: Dockerfile
    image: breakbear-test:latest
    container_name: unlimited-1
    networks:
      - breakbear-test
    labels:
      - "test-type=unlimited"
      - "environment=test"
    restart: unless-stopped

  unlimited-2:
    build:
      context: ..
      dockerfile: Dockerfile
    image: breakbear-test:latest
    container_name: unlimited-2
    networks:
      - breakbear-test
    labels:
      - "test-type=unlimited"
      - "environment=test"
    restart: unless-stopped

networks:
  breakbear-test:
    driver: bridge
    name: breakbear-test